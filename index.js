const { encodeName, first10, last10, getId, decodeId, keccak256, ADDRESS_0 } = require('./src/encodingHelper');

module.exports = {
    encodingHelper: {
        encodeName,
        first10,
        last10,
        getId,
        decodeId,
        keccak256,
        ADDRESS_0,
    },
    encodeName,
    first10,
    last10,
    getId,
    decodeId,
    keccak256,
    ADDRESS_0,
};
